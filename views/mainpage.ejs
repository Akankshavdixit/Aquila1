<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Aquila1</title>
    <link rel="stylesheet" href="style.css">
</head>
<body class="bg-[#EEE0C9] relative">
    
    <a href="/index" class="fixed bottom-8 right-8 bg-[#96B6C5] hover:bg-[#8bc0d8] text-white rounded-full w-16 h-16 flex items-center justify-center shadow-lg">
        <svg class="w-8 h-8" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
            <path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4"></path>
        </svg>
    </a>

    <div class="container mx-auto px-4 py-8">
        
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-8">
            <% data.forEach(item => { %>
                <div class="bg-white p-6 rounded-lg shadow-md border border-gray-200">
                    <p class="mb-4 text-justify <%= item.status === 'completed' ? 'line-through text-gray-500' : '' %>">
                        <%= item.task %>
                    </p>
                    <div class="flex justify-between">
                        <form action="/complete/<%= item.task_id %>" method="POST">
                            <button type="submit" class="text-white bg-green-500 hover:bg-green-700 text-center px-4 py-2 rounded-md">Complete</button>
                        </form>
                        <form action="/delete/<%= item.task_id %>" method="POST">
                            <button type="submit" class="text-white bg-red-500 hover:bg-red-700 text-center px-4 py-2 rounded-md">Delete</button>
                        </form>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>
</body>
</html>
